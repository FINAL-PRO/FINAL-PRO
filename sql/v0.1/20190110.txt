-- 닉네임 체크 프로시져
create or replace PROCEDURE NICK_DUP_CHK
( V_NICK IN DC_MEMBER_TB.NICKNAME%TYPE,
PR_RESULT OUT NUMBER)
IS
BEGIN
    SELECT COUNT(*) INTO PR_RESULT
    FROM DC_MEMBER_TB
    WHERE NICKNAME = V_NICK;
END;