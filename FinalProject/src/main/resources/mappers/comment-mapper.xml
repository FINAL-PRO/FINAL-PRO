<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="comment_mapper">
	
	<!-- 게시글의 댓글 전체 가져오기 -->
	<select id="selectCommentList" resultType="comment" parameterType="_int">
		SELECT * FROM DC_COMMENT_TB WHERE boardNo = #{boardNo}
	</select>
	
	<!-- 댓글 하나 가져오기 -->
	<select id="selectCommentOne" resultType="comment" parameterType="_int">
		SELECT * FROM DC_COMMENT_TB WHERE no = #{no}
	</select>
	
	<!-- 댓글 하나 입력 -->
	<insert id="insertComment" parameterType="comment">
		INSERT INTO DC_COMMENT_TB VALUES(SEQ_COMMENT.NEXTVAL,
			#{content}, SYSDATE, #{memberNo}, #{boardNo}, #{status})
	</insert>
	
</mapper>